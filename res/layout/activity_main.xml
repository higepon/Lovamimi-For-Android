<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".MainActivity" >

    <EditText
        android:id="@+id/timeline_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:ems="10"
        android:inputType="textMultiLine" />

    <RelativeLayout
        android:id="@+id/profile_iconf"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="16dp"
        android:layout_toLeftOf="@+id/timeline_text" >

        <ImageView
            android:id="@+id/profile_image"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignLeft="@+id/timeline_text"
            android:layout_alignTop="@+id/timeline_text"
            android:layout_marginLeft="38dp"
            android:layout_marginTop="37dp"
            android:contentDescription="@string/profile_image"
            android:src="@drawable/ic_launcher" />
    </RelativeLayout>

    <TextView
        android:id="@+id/secret_body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/profile_iconf"
        android:layout_alignLeft="@+id/timeline_text"
        android:layout_alignTop="@+id/profile_iconf"
        android:layout_marginTop="18dp"
        android:textColor="#ff222222"
        android:textIsSelectable="true"
        android:textSize="@dimen/font_size_small" />

</RelativeLayout>